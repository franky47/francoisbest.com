---
title: 'Open Source'
description: A list of open-source projects & packages I contribute to.
layout: PageLayout
---

import { GitHubRepository } from 'src/components/embeds/GitHubRepository'
import { NpmPackageStats } from 'src/components/embeds/NpmPackageStats'

# Open Source

This page lists some of the open source packages and repositories I have published or contribute to.

Most of them are published under my company [47ng](https://github.com/47ng).

## Frontend

My tech stack of choice is [Next.js](https://nextjs.org) and [Chakra UI](https://chakra-ui.com).

Next.js makes it super easy to build static or dynamic webapps, depending on
what is needed.

Chakra UI offers accessibility out of the box with great default styles, that are
easy to override thanks to the styled-system approach.

<GitHubRepository {...require('github:47ng/chakra-next')} />

<NpmPackageStats {...require('npm:@47ng/chakra-next')} mb={12} />

<br />

## Backend

I like to use [Fastify](https://fastify.io) to build backend services in Node.js.

What I like about it:

- More opinionated and structured than Express.js
- Damn fast
- Easy to write plugins
- Good defaults out of the box

<GitHubRepository {...require('github:47ng/fastify-micro')} />

<NpmPackageStats {...require('npm:fastify-micro')} mb={12} />

<GitHubRepository {...require('github:47ng/fastify-cron')} />

<NpmPackageStats {...require('npm:fastify-cron')} mb={12} />

<GitHubRepository
  {...require('github:47ng/actions-clever-cloud')}
  description={
    <p mb={0}>
      GitHub Action to deploy to{' '}
      <a href="https://www.clever-cloud.com"> Clever Cloud</a>.
    </p>
  }
/>

<br />

## Security & Encryption

<GitHubRepository {...require('github:47ng/session-keystore')} />

<NpmPackageStats {...require('npm:session-keystore')} mb={12} />

I wrote an article about how I came to build `session-keystore`:

import { frontMatter as sessionKeystore } from './posts/2019/how-to-store-e2ee-keys-in-the-browser.mdx'

<PostReference {...sessionKeystore} />

<GitHubRepository {...require('github:47ng/cloak')} />

<NpmPackageStats {...require('npm:@47ng/cloak')} mb={12} />

<br />

## Environment Variables

The [Twelve Factor App](https://12factor.net/config) model uses environment
variables extensively for configuration and passing runtime data to a web app.

However, there are things to look out for when working with environment
variables, so I built a few packages to make their management easier and more
secure:

<GitHubRepository {...require('github:47ng/redact-env')} />

<NpmPackageStats {...require('npm:redact-env')} mb={12} />

<GitHubRepository {...require('github:47ng/env-alias')} />

<NpmPackageStats {...require('npm:env-alias')} mb={12} />

<GitHubRepository {...require('github:47ng/check-env')} />

<NpmPackageStats {...require('npm:@47ng/check-env')} mb={12} />

<br />

## Miscellaneous

<GitHubRepository
  {...require('github:FortySevenEffects/arduino_midi_library')}
  description={
    <>
      <p>My longest-running open-source project: MIDI for Arduino.</p>
      <p mb={0}>
        I learned programming in C++ in 2008 with this project and discovered my
        passion for open-source software.
      </p>
    </>
  }
/>

<GitHubRepository {...require('github:47ng/codec')} />

<NpmPackageStats {...require('npm:@47ng/codec')} mb={12} />

<GitHubRepository
  {...require('github:franky47/francoisbest.com')}
  description={
    <p mb={0}>
      The source code for this website! Made with{' '}
      <a href="https://nextjs.org">Next.js</a>,{' '}
      <a href="https://chakra-ui.com">ChakraUI</a> and{' '}
      <a href="https://mdxjs.com/">MDX</a>.
    </p>
  }
/>
